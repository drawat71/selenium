package Pageobjects;

public class useless {

	
	//WebElement scrolldown= driver.findElement(By.xpath("//p[text()='LinkedIn Corporation © 2018']"));
		//JavascriptExecutor js1 = ((JavascriptExecutor) driver);
		//js1.executeScript("window.scrollTo(0, -document.body.scrollHeight);");
		//WebElement scrollup= driver.findElement(By.xpath("//span[@class='search-vertical-filter__dropdown-trigger-text']"));
		//scrollup.click();
	//driver.manage().timeouts().implicitlyWait(20,TimeUnit.SECONDS);
		//js1.executeScript("window.scrollTo(0, -document.body.scrollHeight);");
		//scrolldown.click();
		//scrolldown.sendKeys(Keys.DOWN);
		//scrollup.click();
		//scrollup.sendKeys(Keys.UP);

	//JavascriptExecutor jse = (JavascriptExecutor)driver;
	//jse.executeScript("window.scrollBy(0,250)", "");
	//js.executeScript("arguments[0].scrollIntoView(true);",scrolldown);
	//driver.manage().timeouts().implicitlyWait(90,TimeUnit.SECONDS);
	//js.executeScript("arguments[0].scrollIntoView(true);",scrollup);
	//driver.manage().timeouts().implicitlyWait(90,TimeUnit.SECONDS);
	//joblist=driver.findElements(By.xpath("//h3[@class='job-card-search__title lt-line-clamp lt-line-clamp--multi-line ember-view']"));

}
